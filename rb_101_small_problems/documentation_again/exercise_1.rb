# File::path (class method) returns string representation of file path (returns a string)
# Called on class File

# File#path (instance method) returns the path that was used to create the file the method is called upon as a string
# Called on object of class File

# Class methods are called on classes whereas instance methods are called on objects